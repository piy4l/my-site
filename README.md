quarto preview ---> run locally
quarto render ---> publish and copy files from _site folder

To add a new post later, just create a new folder under posts/ with an index.qmd (title/date/description in the frontmatter) — the listing picks it up automatically on the next quarto render.